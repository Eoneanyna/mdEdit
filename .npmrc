# Electron 与 electron-builder 的二进制默认从 GitHub Release 拉取,国内网络常超时。
# 统一走 npmmirror 镜像,避免 npm install / 打包阶段卡住。
#
# 注意:npm 11 起会对这两个自定义键提示 "Unknown project config",目前仍生效
# (npm 会将其转为 npm_config_* 环境变量传给 electron 的安装脚本),但 npm 12 将停止支持。
# 届时改为在 shell 中导出 ELECTRON_MIRROR 环境变量即可。
# 打包阶段的镜像已在 electron-builder.yml 的 electronDownload 中独立配置,不受此影响。
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
