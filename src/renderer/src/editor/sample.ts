/** 首次启动时展示的语法帮助文档的标题 */
export const SAMPLE_DOC_NAME = '语法帮助'

/**
 * 首次启动展示的内容。既是欢迎页,也是一份可直接照着试的 Markdown 语法速查。
 * 用户点击「新建」后会换成空白的「未命名.md」。
 */
export const SAMPLE_DOC = `# Markdown 语法帮助

欢迎使用 **mdEdit**。这是一个所见即所得的编辑器 —— 输入标记符号后它会立刻变成排版效果,符号本身随即消失。

下面每一节都可以直接在本页试手,改坏了也没关系,这份文档不会被保存到磁盘。

## 标题

行首输入 \`#\` 加空格即可。\`#\` 的个数决定层级,最多六级。

# 一级标题
## 二级标题
### 三级标题
#### 四级标题

左侧边栏切到「大纲」标签,就能看到这些标题构成的目录,点击可跳转。

## 强调

| 想要的效果 | 这样输入 | 结果 |
| --- | --- | --- |
| 加粗 | \`**文字**\` | **文字** |
| 斜体 | \`*文字*\` | *文字* |
| 加粗斜体 | \`***文字***\` | ***文字*** |
| 删除线 | \`~~文字~~\` | ~~文字~~ |
| 行内代码 | \`\\\`文字\\\`\` | \`文字\` |

## 列表

行首输入 \`-\` 加空格是无序列表:

- 第一项
- 第二项
  - 按 Tab 缩进成子项
  - 按 Shift+Tab 退回上一层
- 第三项

行首输入 \`1.\` 加空格是有序列表,序号会自动接续:

1. 第一步
2. 第二步
3. 第三步

输入 \`- [ ]\` 是任务列表,方框可以直接点:

- [x] 已完成的事
- [ ] 待办的事
- [ ] 另一件待办

## 引用

行首输入 \`>\` 加空格:

> 引用可以嵌套,也可以包含其他元素。
>
> > 这是第二层引用。

## 代码

行首输入三个反引号加语言名,回车即可。代码块带语法高亮:

\`\`\`typescript
interface Note {
  title: string
  tags: string[]
}

function greet(name: string): string {
  return \`你好,\${name}\`
}
\`\`\`

\`\`\`python
def fib(n: int) -> int:
    return n if n < 2 else fib(n - 1) + fib(n - 2)
\`\`\`

## 表格

输入 \`|\` 分隔的一行,下一行写 \`| --- |\`,即可成表。保存时列宽会按字符的显示宽度对齐,中文表格的源码也是整齐的。

| 功能 | 快捷键 | 说明 |
| --- | --- | --- |
| 新建 | Ctrl+N | 新建空白文档 |
| 打开文件 | Ctrl+O | 打开单个文件 |
| 打开文件夹 | Ctrl+Shift+O | 作为工作区 |
| 保存 | Ctrl+S | 保存当前文档 |
| 另存为 | Ctrl+Shift+S | 存到新位置 |
| 查找替换 | Ctrl+F | 全文高亮匹配 |
| 大纲 | Ctrl+K | 切换侧边栏标签 |

## 链接与图片

- 链接:\`[显示文字](https://example.com)\` → [显示文字](https://example.com)
- 图片:\`![替代文字](图片路径)\`

链接会交给系统浏览器打开,不会在编辑器内跳转。

## 分割线

单独一行输入三个减号,回车:

---

## 关于保存

这个编辑器在保存时会重新生成 Markdown 源码,因此特意做了几项约束,尽量不改动你原本的写法:

- 列表符号固定用 \`-\`,不会被换成 \`*\`
- 空行仍然是空行,不会被写成 \`<br />\`
- **打开一个文件后直接保存,内容不会有任何变化**

## 其他

- 按 \`/\` 可以唤出块级菜单,快速插入表格、代码块等
- 选中文字会浮出工具条,可直接加粗、加链接
- 「文件」菜单里可以开关**自动保存**(停止输入 2 秒后落盘)
- 文件被其他程序修改时会提示重新载入
- 左侧文件树支持按类型筛选;Word 与 Excel 文件会交给外部程序打开

祝书写愉快。
`
